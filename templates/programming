<!DOCTYPE html>
<html lang="en">
    <head>
        {{ head }}
    </head>
    <body>
        {{ header }}
        <main>
            <div>
                <h1 class="creation-title">{{ data: title }}</h1>
                {{ data: date }}
                {{ data: tags }}
                <h2 class="creation-subtitle" style="margin:0;"><a href="{{ data: link }}" target="blank">alternative link</a></h2>
                <p>{{ data: text }}</p>
            </div>
        </main>
        <footer>
            <p>{{ last_modified }}</p>
        </footer>
    </body>
</html>