<!DOCTYPE html>
<html>
    {{ head }}
    <body>
        {{ header }}
        <main>
            <div class="tagPage" style="width: 80%;">
                <h1>Tagged: {{ gen: tagName }}</h1>
                {{ gen: pageList }}
            </div>
        </main>
        <footer>
            <p>{{ last_modified }}</p>
        </footer>
    </body>
</html>