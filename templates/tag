<!DOCTYPE html>
<html>
    {{ head }}
    <body>
        <!-- navbar div -->
        {{ header }}
        <!-- site content begins -->
        <div class="tagPage main_content">
            <h1>Tagged: {{ gen: tagName }}</h1>
            {{ gen: pageList }}
        </div>
        <!-- fixed footer here -->
        <footer>
            <p class="footer">{{ last_modified }}</p>
        </footer>
    </body>
</html>