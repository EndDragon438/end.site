<!DOCTYPE html>
<html lang="en">
    {{ head }}
    <body>
        {{ header }}
        <main>
            <h1>Blog</h1>
            <ul id="blogPosts">
                {{ gen: blogPosts }}
            </ul>
        </main>
        <footer>
            <p>END IS TYPING...</p>
        </footer>
    </body>
</html>