<!DOCTYPE html>
<html lang="en">
    <head>
        {{ head }}
    </head>
    <body id="top">
        {{ before }}
        {{ header }}
        <main id="content">
            <h1>Blog</h1>
            <ul id="blogPosts">
                {{ gen: blogPosts }}
            </ul>
        </main>
        <footer>
            <p>END IS TYPING...</p>
        </footer>
        {{ after }}
    </body>
</html>